{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python backend/simple_backend.py",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
